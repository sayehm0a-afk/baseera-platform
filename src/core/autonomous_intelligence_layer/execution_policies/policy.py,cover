> from abc import ABC, abstractmethod
> from typing import Any, Dict
  
> class Policy(ABC):
>     """
>     الفئة الأساسية المجردة لجميع سياسات التنفيذ.
  
>     تحدد الواجهة التي يجب أن تتبعها جميع السياسات المحددة.
>     """
  
>     @abstractmethod
>     def apply(self, task_context: Dict[str, Any]) -> Dict[str, Any]:
>         """
>         تطبق هذه السياسة على سياق المهمة المحدد.
  
>         Args:
>             task_context (Dict[str, Any]): سياق المهمة الذي سيتم تطبيق السياسة عليه.
  
>         Returns:
>             Dict[str, Any]: سياق المهمة المعدل بعد تطبيق السياسة.
>         """
!         pass
  
>     @property
>     @abstractmethod
>     def name(self) -> str:
>         """
>         اسم السياسة.
>         """
!         pass
  
>     def __repr__(self) -> str:
!         return f"{self.name}Policy()"
