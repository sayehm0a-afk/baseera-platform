apiVersion: v1
kind: Service
metadata:
  name: basirah-app
  labels:
    app: basirah
    component: app
spec:
  selector:
    app: basirah
    component: app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
