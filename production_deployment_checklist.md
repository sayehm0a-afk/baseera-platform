# قائمة مرجعية لنشر Basirah في الإنتاج

**التاريخ:** 18 يوليو 2026
**المؤلف:** Manus AI

---

## ملخص

توضح هذه القائمة المرجعية الخطوات الأساسية والضرورية لنشر منصة Basirah بنجاح في بيئة إنتاج حقيقية. يجب اتباع كل خطوة بعناية لضمان الاستقرار والأمان والأداء الأمثل.

---

## 1. إعداد البيئة الأساسية (Infrastructure Setup)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 1.1 | **توفير الخوادم/المجموعة:** توفير خوادم (VMs) أو مجموعة Kubernetes (K8s) كافية لتشغيل التطبيق وقاعدة البيانات و Redis. | ⬜ لم يتم | تحديد متطلبات CPU/RAM/Storage في خطة النشر. |
| 1.2 | **تثبيت Docker:** تثبيت Docker Engine و Docker Compose على الخوادم (إذا لم يتم استخدام K8s). | ⬜ لم يتم | إصدار Docker 20.10+ موصى به. |
| 1.3 | **تثبيت kubectl:** تثبيت وتكوين `kubectl` للوصول إلى مجموعة K8s (إذا تم استخدام K8s). | ⬜ لم يتم | |
| 1.4 | **تكوين جدار الحماية:** تكوين جدار الحماية للسماح بالوصول إلى المنافذ المطلوبة (8000 للتطبيق، 5432 لـ PostgreSQL، 6379 لـ Redis). | ⬜ لم يتم | تقييد الوصول إلى المنافذ الداخلية. |
| 1.5 | **تكوين الشبكة:** التأكد من أن الخوادم يمكنها الاتصال ببعضها البعض وبالخدمات الخارجية (OpenAI، مزود بيانات السوق). | ⬜ لم يتم | |
| 1.6 | **تكوين DNS:** إعداد سجلات DNS اللازمة لاسم النطاق الخاص بالتطبيق. | ⬜ لم يتم | |
| 1.7 | **تكوين SSL/TLS:** الحصول على شهادات SSL/TLS وتكوينها لتأمين حركة المرور HTTPS. | ⬜ لم يتم | استخدام Let's Encrypt أو شهادة موثوقة أخرى. |

## 2. إعداد قاعدة البيانات (PostgreSQL Setup)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 2.1 | **تثبيت PostgreSQL:** تثبيت PostgreSQL على خادم مخصص أو استخدام خدمة قاعدة بيانات مُدارة (RDS). | ⬜ لم يتم | إصدار PostgreSQL 13+ موصى به. |
| 2.2 | **إنشاء قاعدة البيانات والمستخدم:** إنشاء قاعدة بيانات `basirah` ومستخدم بامتيازات مناسبة. | ⬜ لم يتم | استخدام بيانات اعتماد آمنة. |
| 2.3 | **تكوين تجميع الاتصالات:** تكوين تجميع الاتصالات (مثل PgBouncer) لتحسين الأداء. | ⬜ لم يتم | |
| 2.4 | **تكوين النسخ الاحتياطي:** إعداد حل للنسخ الاحتياطي التلقائي لقاعدة البيانات. | ⬜ لم يتم | |
| 2.5 | **تكوين المراقبة:** إعداد مراقبة أداء قاعدة البيانات. | ⬜ لم يتم | |
| 2.6 | **تطبيق الترحيلات (Migrations):** تشغيل ترحيلات Alembic لإنشاء مخطط قاعدة البيانات. | ⬜ لم يتم | `alembic upgrade head` |

## 3. إعداد Redis (Redis Setup)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 3.1 | **تثبيت Redis:** تثبيت Redis على خادم مخصص أو استخدام خدمة Redis مُدارة. | ⬜ لم يتم | إصدار Redis 6+ موصى به. |
| 3.2 | **تكوين كلمة المرور:** تعيين كلمة مرور قوية لـ Redis. | ⬜ لم يتم | |
| 3.3 | **تكوين المثابرة:** تكوين مثابرة Redis (RDB أو AOF) لضمان استمرارية البيانات. | ⬜ لم يتم | |
| 3.4 | **تكوين النسخ الاحتياطي:** إعداد حل للنسخ الاحتياطي التلقائي لبيانات Redis. | ⬜ لم يتم | |
| 3.5 | **تكوين المراقبة:** إعداد مراقبة أداء Redis. | ⬜ لم يتم | |

## 4. إعداد التطبيق (Application Setup)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 4.1 | **بناء صورة Docker:** بناء صورة Docker للتطبيق باستخدام `Dockerfile` المتوفر. | ⬜ لم يتم | `docker build -t basirah-app .` |
| 4.2 | **تخزين صورة Docker:** دفع صورة Docker إلى سجل حاويات خاص (مثل Docker Hub الخاص أو AWS ECR). | ⬜ لم يتم | |
| 4.3 | **تكوين متغيرات البيئة:** توفير جميع متغيرات البيئة المطلوبة (خاصة الأسرار) بشكل آمن. | ⬜ لم يتم | استخدام Kubernetes Secrets أو HashiCorp Vault. |
| 4.4 | **النشر (Deployment):** نشر التطبيق باستخدام `docker-compose` أو Kubernetes manifests. | ⬜ لم يتم | `docker-compose up -d` أو `kubectl apply -f kubernetes/` |
| 4.5 | **تكوين فحص الصحة:** التأكد من أن فحص الصحة يعمل بشكل صحيح. | ⬜ لم يتم | التحقق من نقطة نهاية `/health`. |
| 4.6 | **تكوين التسجيل والمراقبة:** دمج التطبيق مع حلول التسجيل والمراقبة المركزية. | ⬜ لم يتم | استخدام ELK Stack، Prometheus/Grafana، أو خدمات سحابية. |

## 5. إعداد الخدمات الخارجية (External Services Setup)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 5.1 | **تكوين OpenAI API:** التأكد من توفير `OPENAI_API_KEY` بشكل آمن. | ⬜ لم يتم | |
| 5.2 | **تكوين مزود بيانات السوق:** توفير بيانات اعتماد مزود بيانات السوق (إذا لزم الأمر). | ⬜ لم يتم | |
| 5.3 | **اختبار الاتصال:** التحقق من أن التطبيق يمكنه الاتصال بنجاح بجميع الخدمات الخارجية. | ⬜ لم يتم | |

## 6. الاختبار والتحقق (Testing & Validation)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 6.1 | **اختبارات الوحدة:** تشغيل جميع اختبارات الوحدة. | ✅ تم | تم التحقق في مرحلة التدقيق المستقل. |
| 6.2 | **اختبارات التكامل:** تشغيل اختبارات التكامل ضد الخدمات الحقيقية. | ⬜ لم يتم | يتطلب بيئة إنتاج حقيقية. |
| 6.3 | **اختبارات الأمان:** إجراء فحص أمان شامل (مثل اختبار الاختراق). | ✅ تم | تم التحقق في مرحلة التدقيق المستقل. |
| 6.4 | **اختبارات الحمل والأداء:** إجراء اختبارات الحمل والأداء. | ✅ تم | تم التحقق في مرحلة التدقيق المستقل. |
| 6.5 | **اختبارات End-to-End:** إجراء اختبارات End-to-End للتحقق من منطق العمل الكامل. | ⬜ لم يتم | يتطلب بيئة إنتاج حقيقية. |
| 6.6 | **التحقق من بيانات السوق:** التحقق من أن بيانات السوق يتم جلبها ومعالجتها بشكل صحيح. | ⬜ لم يتم | يتطلب بيئة إنتاج حقيقية. |

## 7. ما بعد النشر (Post-Deployment)

| الخطوة | الوصف | الحالة | ملاحظات |
|--------|--------|--------|----------|
| 7.1 | **المراقبة المستمرة:** مراقبة أداء التطبيق وصحته بشكل مستمر. | ⬜ لم يتم | |
| 7.2 | **إدارة السجلات:** مراجعة السجلات بانتظام وتكوين التنبيهات. | ⬜ لم يتم | |
| 7.3 | **إدارة التحديثات:** وضع خطة لتحديثات التطبيق والتبعيات. | ⬜ لم يتم | |
| 7.4 | **خطة التعافي من الكوارث:** وضع خطة للتعافي من الكوارث (Disaster Recovery Plan). | ⬜ لم يتم | |
| 7.5 | **التدريب:** تدريب الفريق على تشغيل وصيانة النظام. | ⬜ لم يتم | |

---

**ملاحظة:** هذه القائمة المرجعية هي وثيقة حية ويجب تحديثها بانتظام مع تطور المشروع ومتطلبات البيئة.
