# تدقيق شامل لمنصة بصيرة — 2026-09-11

**نطاق هذا التدقيق:** فحص كامل للواجهة والخادم وقاعدة البيانات، الحسابات والصلاحيات، اتصال بيانات السوق، جدولة المهام، إصدار التوصيات وتتبع نتائجها، الاختبارات والتشغيل. تنفيذه عبر قراءة مباشرة للكود الفعلي في المستودع (`/home/user/baseera-platform`، الفرع الأساسي `main`) + ثلاثة تحقيقات فرعية مستقلة (Auth/RBAC، الواجهة الأمامية، التشغيل/الاختبارات) + تحقق مباشر مني في مسار البيانات والتوصيات وخط زمن السوق، بالإضافة إلى أدلة تراكمية من تدقيق سابق لهذه الجلسة نفسها (توثَّق في `تدقيق_المسح_الطبيعي_ونتائج_التوصيات_2026-09-10.md`).

**تصنيف كل بند أدناه صريح:** موجود ويعمل / موجود لكن ناقص أو به عيب / غير موجود / تم إصلاحه (مع دليل الاختبار) / مقترح لم يُطبَّق / غير متحقَّق.

---

## 1. ملخص الحالة الفعلية والمخاطر الرئيسية

المنصة أعمق وأكثر انضباطًا مما توحي به منصة ناشئة عادة: نظام RBAC صارم مطبَّق على الخادم بالكامل، عزل بيانات مستخدمين سليم، تتبع نتائج توصيات حقيقي وغير قابل للتلاعب بأثر رجعي، وتصميم واعٍ لحدود حصة SAHMK. لكن التدقيق كشف **فجوة تشغيلية حرجة واحدة** وعدة فجوات بيانات/واجهة متوسطة الخطورة.

**أهم ثلاث مخاطر فعلية الآن:**

1. **المسح المتكرر (`MarketIntelligenceScheduler`) متوقف تمامًا في الإنتاج** — لم يُنتج أي فرصة جديدة منذ 2026-09-08 (التشغيلة #140). هذا اكتُشف وأُثبت بأدلة مباشرة يوم أمس (`تدقيق_المسح_الطبيعي_ونتائج_التوصيات_2026-09-10.md`) ومحاولة التحقق منه في staging اصطدمت بعطل نشر منفصل لم يُحل بعد (تفاصيله في نفس الملف). **لم يُصلح، بانتظار قرارك.**
2. **لا يوجد تحقق من صحة بنية شمعة OHLCV** قبل تخزينها واستخدامها في التحليل الفني — عُثر عليه اليوم، **وأُصلح** (البند 3 أدناه).
3. **شاشة "الحساب والباقة" غير موجودة إطلاقًا للمستخدم العادي** — يرى المستخدم حالة حسابه فقط، دون أي معلومة عن باقته أو حدودها، رغم وجود نقطة API خلفية جاهزة (`GET /subscriptions/me`) لم تُربط بالواجهة. **لم يُصلح بعد، مُوثَّق كأولوية تالية.**

---

## 2. خريطة مختصرة للمعمارية وتدفق البيانات

```
SAHMK (مزوّد بيانات خارجي)
   │  (SahmkClient + SahmkRateLimiter: حصة يومية مُقسَّمة CRITICAL > LIVE_SCAN > BACKGROUND)
   ▼
مهام الاستقبال المجدولة (IngestionScheduler: symbols/historical_ohlcv/fundamentals/dividends)
   │  (ترقيم موحّد للأعطال: نفاد حصة ≠ فشل حقيقي)
   ▼
قاعدة البيانات (Postgres) — PriceBar (شموع)، Stock، FundamentalSnapshot، Dividend
   │
   ▼
محرك القرار DecisionEngineV2 (محمي: Brain / Shadow / Recurrent Scan)
   │  (بوابات نشر: جودة بيانات، حداثة قرار، نطاق دخول منطقي...)
   ▼
Radar V2 → RadarOpportunity (التوصية المعروضة للمستخدم)
   │
   ├──▶ الواجهة (فرص اليوم، تفاصيل التوصية، قائمة المتابعة، سجل الأداء)
   └──▶ DecisionV2Outcome (تتبع النتيجة: PENDING → TARGET_HIT/STOP_HIT/EXPIRED)
```

**النظام الموازي المنفصل تمامًا:** جداول legacy (`RecommendationOutcome`/`Snapshot`/`History`) تخص التداول الورقي والاختبار الخلفي (`paper_trading.py`, `backtesting/engine.py`) — لا علاقة لها بتوصيات Radar V2 الحيّة إطلاقًا (تم التحقق سابقًا في هذه الجلسة).

**التخزين المؤقت:** لا Redis مطلوب لعمل النظام الأساسي (يتدهور بأمان لحالة محلية فقط)، لكنه مُستخدم فعليًا في الإنتاج لمشاركة حالة الحصة بين العمليات ولمنع التكرار (rate limiting, revocation, leader lock).

---

## 3. سجل المشاكل (الأعلى خطورة أولًا)

| # | المشكلة | الدليل والموقع | الشدة/الأثر | السبب | الإصلاح | اختبار القبول | الحالة |
|---|---------|------------------|--------------|--------|----------|-----------------|--------|
| 1 | المُجدوِل الدوري لإنتاج التوصيات متوقف في الإنتاج | `market_intelligence_scheduler_running: false` + `MARKET_INTELLIGENCE_SCHEDULER_ENABLED` غير مضبوط، مؤكَّد بقراءتين مستقلتين 2026-09-11 07:33–07:35 UTC | **حرج** — لا فرص جديدة للمستخدم منذ 3 أيام | العلم لم يُفعَّل قط في الإنتاج (وليس فقط نفاد حصة كما ظُنّ سابقًا) | تفعيل العلم في staging أولًا (طلب المستخدم)، ثم الإنتاج بعد موافقة صريحة | مسح جديد يظهر في `last_scan_id > 140` مع أدلة سجل حقيقية | **جارٍ التحقق في staging** — اصطدم بعطل نشر Railway منفصل غير محلول (`dca9c3f7...`)، بانتظار فحص لوحة Railway المباشرة أو توجيه المستخدم |
| 2 | لا تحقق من صحة بنية شمعة OHLCV (high≥low، إغلاق ضمن المدى، حجم غير سالب) | `src/market_data/ingestion/_common.py:upsert_price_bar` (قبل الإصلاح)، لا قيد CHECK على `PriceBar` | **عالٍ** — شمعة تالفة من المزوّد قد تُغذّي التحليل الفني وتُنتج توصية مضلِّلة بصمت | لم يوجد تحقق من الأساس، لا في مستوى التخزين ولا في مستوى المزوّد | إضافة `InvalidPriceBarError` + تحقق بنيوي قبل أي كتابة (خارج منطق المحرك المحمي تمامًا) | 14 اختبارًا في `test_common.py` (7 جديدة) + 23 اختبارًا قائمًا بلا انحدار | **تم إصلاحه واختباره محليًا** — PR #123 (بانتظار CI) |
| 3 | لا يوجد إفصاح صريح أن وقف الخسارة لا يضمن التنفيذ بنفس السعر | `ANALYSIS_DISCLAIMER_AR` (قبل الإصلاح) يذكر فقط "ليس ضمانًا للربح" | **متوسط** — متطلب صريح في المهمة، غيابه قد يُضلِّل مستخدمًا مبتدئًا | نص الإفصاح لم يُحدَّث منذ إضافة حقل `stop_loss` | إضافة جملة صريحة عن الفجوات والسيولة إلى النص المشترك (يظهر تلقائيًا في كل الشاشات الثلاث المستهلكة له) | فحص القيمة الجديدة + لا اختبار Python يعتمد على النص القديم حرفيًا (تم التأكد) | **تم إصلاحه** — PR #123 |
| 4 | لا يوجد شرط تعقيد لكلمة المرور (الطول فقط) | `RegisterRequest.password`/`ResetPasswordRequest.new_password` كانا `Field(min_length=8)` فقط — `"aaaaaaaa"` يمر | **متوسط** | لم يُطبَّق شرط تعقيد قط | حرف + رقم إلزاميان (الحروف العربية تُحتسب)، لا يمس `LoginRequest` إطلاقًا | 9 اختبارات جديدة `test_auth_schemas.py` ناجحة محليًا؛ 3 قيم اختبار تكامل عُدِّلت يدويًا (بانتظار CI الحقيقي لتأكيدها) | **تم إصلاحه، اختبار جزئي غير مكتمل محليًا (لا Postgres في هذه البيئة)** — PR #123 |
| 5 | شاشة "الحساب والباقة" غير موجودة للمستخدم العادي | `frontend/src/app/(app)/settings/page.tsx` يعرض الحساب فقط؛ الوحيد الذي يعرض الاشتراكات هو لوحة الإداري (`owner/subscriptions`) | **متوسط-عالٍ** — متطلب صريح مفقود بالكامل لعامة المستخدمين | لم تُبنَ الواجهة رغم وجود `GET /subscriptions/me` جاهزة في الخادم | ربط نقطة النهاية الموجودة بقسم جديد في صفحة الإعدادات | يظهر للمستخدم: خطة الاشتراك، الحالة، الميزات المتاحة، القيود | **مقترح، لم يُطبَّق بعد** (يحتاج عمل واجهة أمامية لم يتسع له وقت هذه الجولة) |
| 6 | لا سجل تشغيل دائم لمُجدوِل تقييم نتائج القرار (`DecisionV2OutcomeScheduler`) وغيره من مجدولات طبقة الذكاء | `src/ai_evolution/scheduler.py:475-476` يسجّل فقط عبر `logger.info`، لا صف DB لكل دورة | **متوسط** — لا يمكن إثبات "المُجدوِل عمل بنجاح" عبر واجهة إدارية، فقط عبر السجلات النصية | لم يُبنَ جدول تتبع تشغيل لهذا المُجدوِل عند إنشائه (خلافًا لـ`IngestionRunLog`/`MarketScanRun`) | إضافة جدول تشغيل خفيف + عرضه في `/admin/system/summary` | تشغيلة حقيقية تظهر في الاستعلام الإداري | **مقترح، لم يُطبَّق** (يحتاج ترحيل قاعدة بيانات؛ **ملاحظة مهمة:** تحققتُ بشكل مستقل هذه الجلسة من أن النظام فعليًا يعمل وينتج نتائج حقيقية عبر إحصاءات مجمّعة — 27 قرارًا متتبَّعًا، 20 منها محلول، 7 منتهي الصلاحية فعليًا — فالفجوة هنا في **قابلية التدقيق لكل دورة على حدة**، وليست في كون المُجدوِل يعمل أصلًا) |
| 7 | لا مصادقة ثنائية (2FA) لحسابات الإدارة | تم التأكد بالبحث الشامل في `src/` — لا وجود لـ2fa/totp/mfa | **متوسط** | لم تُبنَ | إضافة TOTP اختياري لأدوار OWNER/ADMIN | تسجيل دخول إداري يتطلب رمزًا ثانيًا | **غير موجود، مقترح تحسين** |
| 8 | لا فحص آلي لثغرات الاعتماديات (Dependabot/CodeQL/pip-audit) | لا ملف `.github/dependabot.yml` سابقًا، لا أثر لأي أداة فحص ثغرات | **منخفض-متوسط** | لم يُضَف قط | ملف `dependabot.yml` (pip + npm + github-actions، أسبوعي) | ظهور طلبات سحب تلقائية للتحديثات الأمنية | **تم إصلاحه** — PR #123 |
| 9 | لا يوجد تقويم عطلات رسمية لتداول Tadawul (فجوة مُعلَنة أصلًا في الكود) | `trading_calendar.py`/`market_status.py` يفصحان صراحة عن الفجوة، والحقل `holiday_calendar_disclosed_gap` يصل فعليًا إلى الواجهة (`RealDataStatusBanner`) | **منخفض** — قد تُعرض حالة "السوق مفتوح" خطأً في عطلة رسمية تقع ضمن أحد-خميس | لا مصدر بيانات عطلات متكامل | دمج تقويم عطلات حقيقي (يحتاج مصدر بيانات موثوق) | — | **موجود مع إفصاح صريح للمستخدم بالفعل — ليس عيبًا مخفيًا** |
| 10 | تحديد معدل تسجيل الدخول بحسب IP فقط، ليس بحسب الحساب | `src/api/middleware/rate_limiting.py` — لا `auth_target_key` أو مكافئ | **منخفض** (يُخفَّف بضابط ثانٍ مستقل: قفل الحساب بعد 5 محاولات) | لم يُبنَ تحديد مركّب | إضافة مفتاح تحديد إضافي لكل حساب | — | **موجود لكن ناقص، أولوية منخفضة نظرًا لوجود ضابط تعويضي فعلي** |
| 11 | لا اختبار استعادة فعلي (restore drill) موثَّق لنسخة احتياطية حقيقية | خطة موثَّقة (`docs/architecture/backup-strategy.md`) لكن بلا دليل تنفيذ فعلي لعملية استعادة | **متوسط** | لم يُنفَّذ | إجراء تمرين استعادة تحت إشراف في بيئة معزولة | استعادة ناجحة موثَّقة بالوقت والنتيجة | **غير متحقَّق — يحتاج جلسة عمل مخصصة وموافقة قبل لمس نسخ حقيقية** |
| 12 | ثلاث شاشات متداخلة لعرض الفرص (`today`/`radar`/`opportunities`) | لاحظه تحقيق الواجهة الأمامية | **منخفض** (وضوح منتج، ليس عيبًا وظيفيًا) | تطوّر تدريجي للمنتج | توحيد أو توضيح الفرق للمستخدم | — | **ملاحظة تحسين منتج، لا يحتاج إصلاحًا عاجلًا** |

---

## 4. التغييرات المنفذة فعليًا (هذه الجلسة)

كل التغييرات على فرع مراجعة `audit/data-integrity-and-auth-hardening-2026-09-11`، طُلب دمجها عبر **PR #123** (بانتظار نتيجة CI الحقيقية على Postgres/Redis فعليين، غير متاحين في بيئة التنفيذ المحلية):

1. `src/market_data/ingestion/_common.py` — دالة `_validate_price_bar_data` + استثناء `InvalidPriceBarError` (البند 2 أعلاه).
2. `src/analysis/decision_v2/types.py` — تحديث `ANALYSIS_DISCLAIMER_AR` (البند 3).
3. `src/api/schemas/auth.py` — تحقق تعقيد كلمة المرور (البند 4).
4. `.github/dependabot.yml` — جديد (البند 8).
5. `tests/unit/market_data/ingestion/test_common.py` — 7 اختبارات جديدة.
6. `tests/unit/api/test_auth_schemas.py` — جديد، 9 اختبارات.
7. `tests/integration/api/test_auth_routes.py` — 3 قيم كلمة مرور مُحدَّثة لتوافق القاعدة الجديدة.

**لم يُطبَّق (بقي مقترحًا فقط):**
- إصلاح حصة SAHMK (`MARKET_SCAN` reservation tier) — جاهز ومُختبَر بالكامل (101 اختبار) على فرع منفصل `proposal/market-scan-quota-reservation` منذ الجلسة السابقة — **غير مُطبَّق عمدًا** لأنه بحاجة قرار مستقل حول تفعيل المُجدوِل نفسه أولًا.
- شاشة الحساب والباقة للمستخدم (البند 5).
- سجل تشغيل `DecisionV2OutcomeScheduler` (البند 6).

---

## 5. أدلة الاختبارات

| المجموعة | العدد | النتيجة | ملاحظة |
|---|---|---|---|
| `tests/unit/market_data/ingestion/test_common.py` | 14 | ✅ كلها ناجحة | 7 جديدة لتحقق OHLCV |
| `tests/unit/market_data/ingestion/test_ingest_ohlcv.py` + `test_ingest_historical_ohlcv.py` | 23 | ✅ كلها ناجحة | لا انحدار مع مزوّد dev |
| `tests/unit/api/test_auth_schemas.py` | 9 | ✅ كلها ناجحة | جديد بالكامل، يشمل حالة كلمة مرور عربية |
| `pytest --collect-only` (كامل المستودع) | 4490 | ✅ لا أخطاء تجميع | (نفَّذه تحقيق التشغيل المستقل) |
| اختبارات التكامل المعتمدة على Postgres/Redis حقيقيين | — | **غير متاح محليًا** | بيئة التنفيذ لا تملك خدمات حية؛ CI الفعلي في GitHub يشغّلها ضد Postgres حقيقي — هذا هو مصدر التحقق النهائي، وقد اشتُرك في نشاطه (PR #123) |

**لم يُشغَّل:** اختبار متصفح فعلي (رحلة المدير/المستخدم)، تحميل/أداء تحت ضغط، تمرين استعادة نسخة احتياطية — كلها تحتاج أدوات/بيئة/موافقة غير متاحة في هذا التنفيذ (تفصيل في القسم 9).

---

## 6. مصفوفة الأدوار والصلاحيات والباقات

| الدور | الصلاحيات | التحقق |
|---|---|---|
| **مستخدم (USER)** | وصول لبياناته فقط؛ لا يمكنه اختيار دوره أو ترقيته عبر طلب التسجيل (`RegisterRequest` لا يحتوي حقل دور/صلاحية إطلاقًا) | ✅ مؤكَّد بقراءة الكود |
| **موظف — SUPPORT/ANALYST/ADMIN/OWNER** | تدرّج صلاحيات عبر `require_staff_role`/`require_any_staff_role`، كل مسار إداري تحقَّق يدويًا يحمل الاعتماد الصحيح | ✅ لم يُعثر على أي مسار إداري ينقصه التحقق |
| **ترقية لدور موظف** | حصرًا عبر مسار OWNER-only، ويمنع صراحة أن يُعدِّل OWNER دوره الخاص | ✅ `CannotModifyOwnStaffRoleError` |
| **الباقات/الاشتراك** | نموذج تجربة/اشتراك كامل موجود، `require_active_subscription()` يتحقق من الخادم لا الواجهة فقط | ✅ لكن **لا واجهة تعرضه للمستخدم العادي** (البند 5) |
| **الدفع** | لا مزوّد دفع حقيقي متصل بعد (`NoopPaymentProvider` فقط)؛ لا مسار يمنح اشتراكًا مدفوعًا لمجرد فتح صفحة نجاح | ✅ آمن حاليًا بحكم عدم وجود بوابة حقيقية |
| **عزل البيانات (IDOR)** | قوائم المتابعة، الإشعارات، المحافظ، الجلسات — كلها مُفلترة بمعرّف المستخدم الحالي من الخادم | ✅ لم يُعثر على أي ثغرة IDOR |
| **سجل الإجراءات الإدارية** | `AuditLog` موجود ومُفعَّل فعليًا (20+ نقطة استدعاء)، متاح للمراجعة عبر مسار إداري | ✅ |

---

## 7. سجل التوصيات ومنهجية التقييم (موثَّق سابقًا هذه الجلسة، مُلخَّص هنا)

- كل قرار فعلي (`BUY_CANDIDATE`/`STRONG_BUY_CANDIDATE`) يحصل تلقائيًا على سجل `DecisionV2Outcome` — الحالات الأخرى (`WATCH`/`HOLD`/`WAIT_FOR_ENTRY`/`REJECT`/`REDUCE`) **لا تحصل على سجل نتيجة بالتصميم**، وهذا سلوك مقصود موثَّق، لا عطل.
- مدة الصلاحية الافتراضية 30 يومًا (`DECISION_V2_OUTCOME_DEFAULT_HORIZON_DAYS`) — تم التحقق عمليًا من أنها تعمل (7 حالات منتهية فعليًا من أصل 27 قرارًا متتبَّعًا تاريخيًا حتى 2026-09-10).
- **الحالات المصنَّفة:** `PENDING` → `TARGET_HIT`/`STOP_LOSS_HIT`/`EXPIRED` — لا يوجد استبدال للتوصية الأصلية بعد ظهور النتيجة (كل تحديث حدث جديد موثَّق، مؤكَّد من تصميم `RecommendationHistoryPanel`'s "never hide losses" في الواجهة).
- **لم يُتحقَّق بعد في هذه الجلسة:** منهجية معالجة الفجوات السعرية، الأهداف/الوقف الظاهرين في نفس الشمعة، التنفيذ الجزئي — هذه قواعد تقييم دقيقة تحتاج قراءة متعمقة لمحرك `outcome_evaluation` نفسه لم تتسع لها هذه الجولة؛ **يُوصى بجولة تدقيق منفصلة مخصصة لهذا الملف تحديدًا** قبل الاعتماد على أرقام الأداء في أي قرار تسويقي.
- **لا نسبة نجاح تُعرض هنا أو في الواجهة كرقم نهائي** — العينة الحالية (27 قرارًا) أقل من الحد الأدنى المطلوب (30) والحالة رسميًا `PRELIMINARY`؛ عرض نسبة الآن سيكون تضليلًا إحصائيًا.

---

## 8. نتائج مقارنة أي تحسين تجريبي على المحرك

**لم يُجرَ أي تحسين تجريبي على منطق القرار في هذه الجولة، وهذا متعمَّد.** طُبِّق فقط إصلاح واحد يمسّ حصة الموارد (وليس منطق القرار) — نقل أولوية طلبات SAHMK للمسح الدوري من `BACKGROUND` إلى مستوى جديد `MARKET_SCAN` — وهو **مُجهَّز ومُختبَر بالكامل (101 اختبار) لكن غير مُطبَّق**، بانتظار قرار منفصل حول تفعيل المُجدوِل نفسه أولًا (البند 1). لا مقارنة أداء نسختين مطروحة لأن لا نسخة تجريبية من محرك القرار وُجدت أو أُنشئت.

---

## 9. ما لم يُختبر أو لم يثبت بعد — بصراحة كاملة

| البند | لماذا لم يُختبَر | ما يلزم لإكماله |
|---|---|---|
| رحلة المدير الفعلية عبر متصفح حقيقي | لا أداة متصفح مصرَّح بها متاحة في بيئة هذا التنفيذ | جلسة مخصصة بأداة تصفح + حساب اختبار معزول |
| رحلة المستخدم الجديد (تسجيل → تحقق → بحث → متابعة) عبر متصفح | نفس السبب أعلاه | نفس المتطلب |
| اختبار Safari تحديدًا | نفس السبب | نفس المتطلب |
| اختبارات التكامل الثلاثة المُعدَّلة في `test_auth_routes.py` | لا Postgres/Redis حي في بيئة التنفيذ المحلية | نتيجة CI الفعلي على PR #123 (مُشترَك بنشاطه، سيصل تلقائيًا) |
| اختبار تحميل/ضغط مباشر | موجود أداة محدودة النطاق (`production-load-acceptance.yml`) لكن لم تُشغَّل في هذه الجولة | تشغيلها يدويًا (لا تستهلك حصة SAHMK حسب توثيقها) |
| تمرين استعادة نسخة احتياطية حقيقي | إجراء حساس على بيانات حقيقية، يحتاج موافقة وبيئة معزولة | جلسة عمل مخصصة بعد موافقة صريحة |
| منهجية تقييم النتائج الدقيقة (فجوات، تنفيذ جزئي) | لم تتسع الجولة لقراءة عميقة لملف `outcome_evaluation` كاملًا | جولة تدقيق منفصلة مخصصة |
| تفعيل `MARKET_INTELLIGENCE_SCHEDULER_ENABLED` في staging | اصطدم بعطل نشر Railway (بناء ناجح، تشغيل فاشل) لم يُشخَّص سببه الحقيقي بعد | فحص لوحة Railway المباشرة (البند 1) |

---

## 10. خطة التراجع والنشر المقترحة

- **PR #123 (هذه الجولة):** أربعة إصلاحات مستقلة، لا تمس محرك القرار. التراجع: `git revert` لأي commit بمفرده دون أثر على البقية (كل إصلاح مستقل تمامًا عن الآخر).
- **قبل الدمج:** انتظار نتيجة CI الحقيقية (الاختبارات الثلاثة المعدَّلة تحديدًا).
- **لا نشر إنتاجي مطلوب لهذا PR** فور الدمج — لا شيء هنا يغيّر سلوك التطبيق المنشور فعليًا حتى يُدفع نشر منفصل (`deploy-railway.yml` بتشغيل يدوي فقط، لا نشر تلقائي عند الدمج).
- **إصلاح حصة SAHMK (`MARKET_SCAN`):** جاهز على فرع منفصل، **لن يُدمج أو يُنشر دون عرضه أثره الدقيق عليك أولًا** (الحصة اليومية للخلفية تنخفض من 50 إلى 35 طلبًا في أسوأ الحالات — موثَّق بالكامل في الفرع).
- **تفعيل المُجدوِل في الإنتاج:** قرار منفصل تمامًا، لن يُتَّخذ دون موافقتك الصريحة، وبانتظار حل عطل التحقق في staging أولًا.

---

## 11. تعليمات تشغيل ومتابعة مختصرة

1. **راقب PR #123** — عند اكتمال CI، إن فشل أي اختبار من الثلاثة المعدَّلة يدويًا، سيُصلَح فورًا (هذه الجلسة مشتركة بنشاط الـPR).
2. **قرارك مطلوب على 3 أمور متوقفة:**
   - فتح لوحة Railway لفحص سبب فشل نشر `MARKET_INTELLIGENCE_SCHEDULER_ENABLED` في staging (تفاصيل في `تدقيق_المسح_الطبيعي_ونتائج_التوصيات_2026-09-10.md`).
   - الموافقة (أو الرفض) على تطبيق إصلاح حصة `MARKET_SCAN` على فرعه الحالي.
   - الموافقة على تفعيل المُجدوِل في الإنتاج بعد نجاح التحقق في staging.
3. **الأولوية التالية الموصى بها بعد ما سبق:** بناء شاشة "الحساب والباقة" للمستخدم العادي (البند 5) — تغيير واجهة أمامية فقط، يربط نقطة نهاية جاهزة أصلًا.
4. **لا تُهمَل:** تمرين استعادة نسخة احتياطية حقيقي (لم يُثبَت قط)، وجولة تدقيق منفصلة لملف تقييم نتائج التوصيات (`outcome_evaluation`).

---

## الحكم النهائي (منفصل بالضرورة، غير مختزل في رقم واحد)

- **جاهزية البيانات:** جزئية — مصدر واضح وموثوق (SAHMK)، توقيت وحالة سوق مُفصَح عنهما بدقة تصل للواجهة فعليًا، لكن **مصدر التوصيات الجديدة نفسه متوقف حاليًا** (البند 1) وتحقق بنية الشمعة كان غائبًا (أُصلح اليوم).
- **جاهزية الوظائف والواجهات:** جيدة إلى قوية — كل الشاشات الرئيسية موجودة وتعمل بعربية RTL سليمة وحالات فراغ/خطأ حقيقية، عدا شاشة الباقة (غائبة) وبعض النصوص التوضيحية الناقصة (إفصاح الوقف، أُصلح اليوم).
- **جاهزية الحسابات والصلاحيات:** قوية جدًا — لم يُعثر على أي ثغرة RBAC أو IDOR حقيقية، سجل تدقيق فعلي، عزل بيانات سليم. الفجوات المتبقية (2FA، تحديد معدل بالحساب) تحسينات لا عيوب حرجة.
- **مستوى الدليل على جودة التوصيات:** أولي (PRELIMINARY) بتصريح النظام نفسه — 27 قرارًا متتبَّعًا فقط، أقل من الحد الأدنى الإحصائي (30)، ولا نسبة نجاح تُعرض عن حق. آلية التتبع نفسها سليمة ومُثبتة العمل، لكن حجم العينة لا يكفي لأي حكم أداء بعد — و**لا فرص جديدة تُضاف للعينة حاليًا** بسبب البند 1.
- **جاهزية الإطلاق:** **غير جاهزة للإطلاق التجاري الآن** — العائق الحاسم هو توقف مصدر التوصيات الجديدة (البند 1)، يليه غياب شاشة الباقة وعدم اختبار رحلتي المستخدم/المدير فعليًا عبر متصفح. لا مانع تقني من مواصلة العمل الداخلي والتطوير بالتوازي مع حل هذه البنود.
