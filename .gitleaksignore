# Gitleaks ignore list for Basirah.
#
# Each line is a fingerprint (commit:file:rule-id:start-line) for one
# specific, individually-reviewed finding -- never a broad pattern,
# path, or rule-wide exemption. This is gitleaks' own supported
# mechanism for a confirmed false positive that lives in git history
# and cannot be edited without rewriting published commits (a
# destructive operation this repository's workflow deliberately
# avoids). It does not disable or weaken any detection rule; every
# other finding gitleaks' default ruleset can produce is still
# reported normally.
#
# NEVER add a fingerprint here without first confirming, by manual
# review of the actual diff at that commit, that the flagged string is
# not a real secret. NEVER add an entry for a rule you have not
# personally verified.

# tests/unit/market_data/test_config.py:12 -- a pytest fixture that
# uses monkeypatch to point the SAHMK API key environment variable at
# a low-entropy, obviously-synthetic placeholder (it literally says
# "test" in it) while asserting src.market_data.config's reader
# function reads back whatever the environment provides. That
# placeholder has never been a valid SAHMK credential -- confirmed by
# reading the actual diff at this commit before adding this line. The
# exact source text is deliberately not quoted here (an earlier
# revision of this comment did quote it verbatim and that alone
# produced a second, self-inflicted generic-api-key match on this
# very file -- see git history if curious).
a28c7360c6fbb82d8fba5517a3bbcb824cdc63c2:tests/unit/market_data/test_config.py:generic-api-key:12

# .gitleaksignore itself, line 19 of an earlier revision of this file
# (before the reword above) -- that revision's explanatory comment
# quoted the finding directly above verbatim, in the same quoted
# key-then-value shape the generic-api-key rule looks for, which
# produced this second, purely self-inflicted match on the comment
# text itself rather than on any code or credential. No secret was
# ever present at that historical line either; the content there was
# prose describing the first finding, not a key assignment of any
# kind. Fixed going forward by no longer quoting flagged source text
# verbatim in this file, but the historical commit that once contained
# the quote cannot be edited without rewriting published history, so
# it is allowlisted the same way as every other entry here.
d0c36ac715b4fdb7173092380d40f16ce9de2dee:.gitleaksignore:generic-api-key:19

# PR113 (quality-proof instrumentation hardening / P1-1 remediation):
# three findings, all the identical false-positive shape -- an
# assertion or assignment reading the `independent_signal_key`
# attribute of a DecisionV2Outcome ORM object against itself or another
# instance. That attribute is a domain field (a UUID4 stored in a
# nullable String(36) column) used purely to group related quality-
# proof outcome rows for statistical-independence tracking -- it holds
# no credential, token, password, bearer value, or secret of any kind.
# generic-api-key is matching on the substring "key" in the Python
# attribute name itself, not on any actual key material. Each line
# below was individually confirmed by reading the real source diff at
# its commit before being added; none of the three lines is quoted
# here verbatim, for the same self-inflicted-match reason documented
# above.
e6aabb5b78bfa4c262965eeb968c57391870a1b5:tests/integration/test_decision_v2_outcome_concurrency.py:generic-api-key:123
99c1569b6802552375ce9ecb75c581e755ac2e12:tests/unit/ai_evolution/test_decision_v2_outcome.py:generic-api-key:537
99c1569b6802552375ce9ecb75c581e755ac2e12:tests/unit/ai_evolution/test_decision_v2_outcome.py:generic-api-key:643

# Same three findings as above, re-flagged under a DIFFERENT commit SHA
# because this repository squash-merges PRs: PR #113's original commits
# (e6aabb5, 99c1569, on the feature branch) never became part of main's
# history -- only their squash-merge commit did, under a NEW SHA
# (d6401c5). Gitleaks fingerprints are commit-specific, so the squash
# commit needs its own allowlist entries even though the content is
# byte-for-byte identical and was already reviewed above. Confirmed
# again directly against the current source at this commit (PR #120
# review) before adding -- same three independent_signal_key domain-
# field accesses, still no credential of any kind. This squash-merge/
# fingerprint interaction will recur for any future PR whose gitleaks
# scan reaches this squash commit; a more durable fix (e.g. a path- and
# rule-scoped exception instead of per-commit fingerprints) is out of
# this PR's scope.
d6401c5c67e45fc65d7aa3cd316edb33a5e7a782:tests/integration/test_decision_v2_outcome_concurrency.py:generic-api-key:123
d6401c5c67e45fc65d7aa3cd316edb33a5e7a782:tests/unit/ai_evolution/test_decision_v2_outcome.py:generic-api-key:537
d6401c5c67e45fc65d7aa3cd316edb33a5e7a782:tests/unit/ai_evolution/test_decision_v2_outcome.py:generic-api-key:643
